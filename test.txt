/* Now the interpreter is in place, we can ignore the majority of this until
   the entire language has been implemented.

// This is a test file for the JLox scanner
// Testing single line comments first.

var a = 10;

for (;;) {
  var b = 20;

  a += b;
 /*
  * Testing multi-line, C-style comments
  * ensuring we skip them in the scanner
  */

  /*
   *  /* This is an embedded c-ctyle comment */
   */

  if (a > 5000) {
    break;
  }
}
*/

/* // Simple statement tests...
print "one";
print "two";
print true;
print !true;
print "2 + 3 = " + (2+3);
*/

/* // Variable scope and code block testing
var a = "global a";
var b = "global b";
var c = "global c";
{
  var a = "outer a";
  var b = "outer b";
  {
    var a = "inner a";
    print a;
    print b;
    print c;
  }
  print a;
  print b;
  print c;
}
print a;
print b;
print c;
*/

// For loop testing...
var a = 0;
var tmp;

for (var b = 1; a < 10000; b = tmp + b) {
  print a;
  tmp = a;
  a = b;
}
