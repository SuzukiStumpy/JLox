class Circle {
  init(radius) {
    this.radius = radius;
  }

  area {
    return 3.1415926535 * this.radius * this.radius;
  }
}

var circle = Circle(4);
print circle.area; // Prints approx "50.2655"
